# [apps/frontend] production env — VPS path: apps/frontend/.env
# Used as documentation; image build gets VITE_API_BASE_URL from root compose .env
#
#   cp apps/frontend/.env.example apps/frontend/.env

VITE_API_BASE_URL=https://selene.dankehidayat.my.id/api
