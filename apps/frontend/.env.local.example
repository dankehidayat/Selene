# [apps/frontend] .env.local.example
# Copy this file to .env for local development on macOS
#
# Prefer the Vite proxy path so the browser talks same-origin and avoids
# CORS failures when you open 127.0.0.1 vs localhost.
VITE_API_BASE_URL=/api